# 서버 포트 (Render는 자동 제공, 로컬은 3000 권장)
PORT=3000

# 허용 오리진 (쉼표 구분). 비워두면 모든 오리진 허용 (개발 편의).
ALLOWED_ORIGINS=http://localhost:3000

# 분당 허용 요청 수 (기본 120)
RATE_LIMIT_MAX=120

# 클라이언트에서 소켓 주소를 명시적으로 지정하고 싶을 때(예: 프록시/커스텀 도메인)
# NEXT_PUBLIC_SOCKET_URL=https://yourapp.onrender.com
