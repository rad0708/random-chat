# 필수
PORT=3000
# 배포 환경에서 자동 주입됨(Render/Railway는 PORT 제공)

# 선택 - MongoDB 사용 시
USE_DB=true
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority

# 운영 정책
MAX_MSG_PER_MIN=60
MATCH_TIMEOUT_SEC=30
CHAT_MAX_MINUTES=30
ALLOWED_ORIGINS=*
