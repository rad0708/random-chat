{
  "name": "shufflechat-monorepo",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm --workspace server run dev & npm --workspace client run dev",
    "build": "npm --workspace server run build && npm --workspace client run build",
    "start": "node server/dist/index.js"
  }
}