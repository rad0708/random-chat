{
  "name": "random-chat-final",
  "version": "1.0.0",
  "description": "Minimal but polished random chat with hCaptcha and ads (DB-free), ready for Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "node-fetch": "^3.3.2"
  }
}