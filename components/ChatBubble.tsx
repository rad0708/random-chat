'use client';
export function ChatBubble({isOwn,nickname,message,timestamp}:{isOwn:boolean;nickname:string;message:string;timestamp:string;}){return(<div className={`flex w-full flex-col gap-1 ${isOwn?'items-end text-right':'items-start text-left'}`}><span className="text-xs text-gray-500">{nickname} · {new Date(timestamp).toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'})}</span><div className={`max-w-[80%] rounded-2xl px-4 py-3 text-sm shadow ${isOwn?'rounded-br-sm bg-primary text-white':'rounded-bl-sm bg-white text-gray-900'}`} role="text" aria-label={`${nickname}의 메시지`}>{message}</div></div>);}
