'use client';

// 간단한 배너 자리 표시자 컴포넌트입니다.
// 실제 광고 네트워크 스니펫으로 교체하세요.
export function AdBanner() {
  return (
    <div className="w-full rounded-xl border border-dashed border-gray-300 bg-white p-6 text-center text-sm text-gray-500">
      광고 영역 (ads.txt 설정 필요)
    </div>
  );
}
